import styled from "styled-components";
import { theme } from "../../../styles/theme";

export const Divisor = styled.hr`
    width: 80vw;
    margin: 25px 0;
    background-color: ${theme.colors.quartenaryColor};
    height: 1px;
    border: none;
`