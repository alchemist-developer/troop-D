import styled from 'styled-components';

export const Parent = styled.div`
  position: relative;
  top: 0px;
  right: -45px;
  `

  export const Child = styled.div`
  position: absolute;
  bottom: -4px;
  left: -60px;
  `